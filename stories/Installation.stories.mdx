import {Meta, Subtitle} from '@storybook/addon-docs';

<Meta
  title="Installation"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: {hidden: true},
    },
  }}
/>

# Installation

```sh
yarn add @cermati/serasi
```

### Import components:

```jsx
import {Button} from '@cermati/serasi';

export default (props) => {
  return <Button color="danger">Danger!</Button>;
};
```

...Coming soon
